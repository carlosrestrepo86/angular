import { EjemploDirective } from './ejemplo.directive';

describe('EjemploDirective', () => {
  it('should create an instance', () => {
    const directive = new EjemploDirective();
    expect(directive).toBeTruthy();
  });
});
